using System;

namespace SingletonApp
{
    class Program
    {
        static void Main(string[] args)
        {
            ////singleton
            //Singleton obj1 = Singleton.GetInstance;
            ////obj1.Print("i m single obj1");

            //Singleton obj2 = Singleton.GetInstance;
            ////obj2.Print("i m single obj2");

            ////Singleton obj = new Singleton();


            //Polymorphism
            #region Polymorphism
            //Polymorphism.main();
            #endregion

            //ValueType_ReferenceType
            #region ValueType_ReferenceType
            ValueType_ReferenceType.main();
            #endregion

            Console.ReadLine();
        }
    }
}
